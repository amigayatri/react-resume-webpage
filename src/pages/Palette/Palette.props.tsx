import Palette from "."
import PageProps from "../../types/PageProps"

const PaletteProps: PageProps = {
	path: "/palette/",
	element: Palette,
	id: "palette",
	icon: "pantone",
	isHome: false
}

export default PaletteProps
