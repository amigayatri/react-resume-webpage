import { iconKey } from "../common"
import { AccentPair } from "../colors"

export type musicPath = string
export type musicName = string
export type musicAccentColor = AccentPair
export type MusicInfo = [musicName, iconKey, string]
