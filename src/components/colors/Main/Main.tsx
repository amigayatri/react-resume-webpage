import { MainWrapper } from "./Styles";
import { AddColor } from "../AddColor/AddColor";

export const Main = ( ) => {
    return (
        <MainWrapper>
            <AddColor></AddColor>
        </MainWrapper>
    );
}