import { MainWrapper } from "./Styles";
import { PageList } from "../PageList/PageList";

export const Main = ( ) => {
    return (
        <MainWrapper>
            <PageList />
        </MainWrapper>
    );
}