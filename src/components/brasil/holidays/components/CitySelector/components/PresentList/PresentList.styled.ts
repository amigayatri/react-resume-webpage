"use client"

import styled from "styled-components"

export const PresentListWrapper = styled.div`
	list-style: none;
	padding: 0;
`

export const Title = styled.span``
export const ListWrapper = styled.ul``
