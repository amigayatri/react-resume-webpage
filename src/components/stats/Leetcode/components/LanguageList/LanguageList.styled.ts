"use client"

import styled from "styled-components"

export const LanguageListWrapper = styled.section`
	max-width: var(--max-leetcode);
	margin: 32px auto;
`

export const List = styled.ul`
	display: flex;
	list-style: none;
	margin: 32px 0;
	align-items: center;
	justify-content: space-evenly;
`
