"use client"

import styled from "styled-components"

export const IDListWrapper = styled.ul`
	list-style: none;
	padding: 0;
	text-align: left;
`

export const IDWrapper = styled.li`
	height: max-content;
	display: inline-flex;
	justify-content: center;
	align-items: center;
	width: fit-content;
	padding: 4px;
	border: 2px solid currentColor;
	border-radius: 8px;
	margin: 8px 8px 0 0;
`

export const ID = styled.span`
	display: inline-block;
	margin: 0 8px;
	font-size: 1.25rem;
`

export const Subtitle = styled.h2``
