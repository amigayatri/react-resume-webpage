import { shadeGenerator } from "./generator"
import { addTarget, removeTarget, getTargets } from "./targets"
import { getSteps, setSteps } from "./steps"
import { createPalette } from "./palette"
import { getVariations } from "./variations"
export {
	getVariations,
	shadeGenerator,
	addTarget,
	removeTarget,
	getTargets,
	getSteps,
	setSteps,
	createPalette
}
