"use client"

import { MulticoloredNameProps } from "."
import { MulticoloredNameBase } from "./MulticoloredNameBase"

export const MulticoloredName = (props: MulticoloredNameProps) => {
	return <MulticoloredNameBase {...props} />
}
